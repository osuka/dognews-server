# This file exists only to give pytest a top-level package

# When pytest is run it finds test files and then goes up looking for the first
# folder without __init__.py and sets the last one as the base package

